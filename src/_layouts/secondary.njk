<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- https://github.com/joshbuchea/HEAD -->
    <!-- Recommended -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Meta -->
    <meta name="description" content="{{ metadata.description }}" />
    <meta name="generator" content="{{ eleventy.generator }}" />
    <!-- Icons -->
    <!-- https://favicon.io/ -->
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="{{ '/apple-touch-icon.png' | url }}"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="{{ '/favicon-32x32.png' | url }}"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="{{ '/favicon-16x16.png' | url }}"
    />
    <link rel="manifest" href="{{ '/site.webmanifest' | url }}" />
    <title>{{ title or metadata.title }}</title>

    <!--link rel="stylesheet" href="{{ 'secondary.css' | url }}" /-->

    <style>
      {% set css %}
      {% include "../_assets/css/styles.css" %}
      {% endset %}
      {{css | postcss | safe}}
    </style>

  </head>
  <body class="font-mono leading-normal tracking-normal text-gray-900">
  <p>
    {{ content | safe }}
  </p>
    <footer class="bg-red-200 text-center p-4 fixed bottom-0 w-full">
  ¯\_(ツ)_/¯ made with ♥
</footer>

  </body>
</html>